# Rust入门

1.变量默认不可修改

```
fn main() {
    // ---- 变量 ----
    let x = 5; 
    // x = 6; // 如果取消注释，编译器会报错！因为它默认不可变

    let mut y = 10; // 加上 mut，它才能被修改
    y = 15; 
    
    // ---- 常量 ----
    // 常量必须写类型，且全大写（CTF 中常用于存放偏移量或固定 Key）
    const MAX_POINTS: u32 = 100_000;
}
```

2.数据类型：强类型，但可以部分推导

```
fn main() {
    let a: i32 = -10;   // 有符号整数 (i8, i16, i32, i64, i128)
    let b: u32 = 20;    // 无符号整数 (u8, u16, u32, u64, u128) -> CTF 处理字节常用 u8
    let c: f64 = 3.14;  // 浮点数
    let is_pwn: bool = true;
    let heart_eye = '😻'; // 字符 (char)，是 4 字节的 Unicode
}
```

3.数据结构：字符串和数组：String和Vec

```
fn main() {
    // ---- String ----
    let mut s = String::from("Hello"); // 创建
    s.push_str(" CTF");              // 追加字符串
    s.push('!');                     // 追加字符
    println!("{}", s);               // 打印结果：Hello CTF!

    // ---- Vec (数组) ----
    let mut v = vec![1, 2, 3];       // 创建一个初始数组
    v.push(4);                       // 添加元素
    println!("第二个元素是: {}", v[1]); 
}
```

4.流程控制：if和loop

```
fn main() {
    let number = 7;

    // 基础用法
    if number < 10 {
        println!("是个个位数");
    } else {
        println!("是个大数");
    }

    // 像三元运算符一样的用法
    let condition = true;
    let result = if condition { "通过" } else { "挂了" }; // 返回值
    println!("结果是: {}", result);
}
```

5.for循环

```
fn main() {
    // 遍历 0 到 4 (不含 5)
    for i in 0..5 {
        println!("次数: {}", i);
    }

    // 遍历一个数组
    let brands = ["Pwn", "Reverse", "Crypto"];
    for item in brands.iter() {
        println!("方向: {}", item);
    }
}
```

6.函数

```
// 定义一个加法函数
// -> 后面是返回类型
fn add(a: i32, b: i32) -> i32 {
    a + b  // 注意这里没分号，代表这是返回值
}

fn main() {
    let sum = add(5, 6);
    println!("5 + 6 = {}", sum);
}
```

